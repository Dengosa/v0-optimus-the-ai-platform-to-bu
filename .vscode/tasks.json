{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Next.js: dev (port 3000)",
      "type": "shell",
      "command": "npx next dev --port 3000",
      "isBackground": true,
      "problemMatcher": [
        "$tsc-watch"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}

